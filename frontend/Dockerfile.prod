# Production stage - uses pre-built files
FROM nginx:alpine

# Install apache2-utils for htpasswd support
RUN apk add --no-cache apache2-utils

# Copy pre-built files (build locally first with 'npm run build')
COPY dist /usr/share/nginx/html

# Expose ports
EXPOSE 80 443

CMD ["nginx", "-g", "daemon off;"]
